apiVersion: v1
kind: Service
metadata:
  name: {{ include "deploy_to_eks.fullname" . }}
  labels:
    {{- include "deploy_to_eks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deploy_to_eks.selectorLabels" . | nindent 4 }}
